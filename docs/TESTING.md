# Testing

## 🧪 Testing

```bash
# Backend — pytest
cd backend
pytest -x -q

# Frontend — Vitest unit tests
cd frontend
npm test

# Frontend — type check + lint
npx tsc --noEmit
npx next lint

# Frontend — Playwright e2e
npx playwright test
```

